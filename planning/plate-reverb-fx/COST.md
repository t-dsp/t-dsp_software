# hexefx FX cost table

Auto-generated by `tools/fx_cost.py`. Deltas are vs the `fx_none` baseline
(tone → DAC, no effect). One effect instantiated per build; `--gc-sections`
strips the rest, so each row is that effect's real marginal cost.

**Baseline `fx_none`:** FLASH 65.0 KB · RAM1 75.9 KB · static RAM2 22.7 KB

Columns: **ΔFLASH / ΔRAM1(static)** from `teensy_size` (build-only). **CPU% / heapRAM2(run)** are runtime, hardware-measured (need `--port`) — the effects `malloc()` their delay buffers from the RAM2 heap, which the static report can't see, so `heapRAM2` is the number that really decides RAM fit.

| effect | ΔFLASH KB | ΔRAM1 KB (static) | CPU % | heapRAM2 KB (run) | memF32 blk | build |
|---|--:|--:|--:|--:|--:|:--|
| fx_delay | 5.0 | 0.4 | 8.2 | 300.0 | 3 | ✅ |
| fx_phaser | 1.0 | 0.3 | 1.8 | 0.0 | 3 | ✅ |
| fx_plate | 8.0 | 1.9 | 10.5 | 190.8 | 3 | ✅ |
| fx_reverbsc | 2.0 | 0.5 | — | — | — | ✅ |
| fx_spring | 5.0 | 0.5 | 14.3 | 37.0 | 3 | ✅ |

_CPU% and heapRAM2 are blank unless run with `--port COMx` (they need the board)._
_ReverbSC allocs its ~396 KB buffer from **PSRAM** (`use_psram=true`), so its heapRAM2_
_delta stays low but it needs a PSRAM board to run at all._
